---
import { fetchProgress, type Version } from "../lib/progress";

interface Props {
  version: Version
}

const colors = {
  green: { stroke: "#40e334", fill: "#91eb7f" },
  yellow: { stroke: "#e3ac34", fill: "#edc97e" },
  blue: { stroke: "#3d76da", fill: "#1e3b6d" },
  orange: { stroke: "#cc8400", fill: "#ffa500" },
};

console.log(await fetchProgress(Astro.props.version));
---
